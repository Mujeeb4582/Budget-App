<div class="d-flex flex-column justify-content-center">
    <h1 class="font-weight-bold text-center display-2">Budget Tracking</h1>

  <div class="d-flex flex-wrap justify-content-center align-items-center gap-4 py-3">
    <form action="<%= new_user_session_path %>" method="get">
      <button class="btn btn-primary btn-md">LOG IN</button>
    </form>
    <form action="<%= new_user_registration_path %>" method="get">
      <button class="btn btn-md btn-outline-primary">SIGN UP</button>
    </form>
  </div>
</div>
